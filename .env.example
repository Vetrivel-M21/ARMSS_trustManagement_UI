# Base URL of the backend API, including the /api/v1 prefix.
# Leave unset for local development (defaults to http://localhost:8080/api/v1).
# Set this in .env.production (or the deployment host's env config) to the
# deployed backend's URL, e.g. https://api.yourtrust.org/api/v1
VITE_API_BASE_URL=
